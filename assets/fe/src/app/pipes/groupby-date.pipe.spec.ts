import { GroupbyDatePipe } from './groupby-date.pipe';

describe('GroupbyDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupbyDatePipe();
    expect(pipe).toBeTruthy();
  });
});
