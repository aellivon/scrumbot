import { GroupIssuesPipe } from './group-issues.pipe';

describe('GroupIssuesPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupIssuesPipe();
    expect(pipe).toBeTruthy();
  });
});
